import {Component,inject,OnInit} from '@angular/core';import {CommonModule} from '@angular/common';import {AdminShellComponent} from '../../shared/admin-shell.component';import {AdminService} from '../../../../core/services/admin.service';
@Component({selector:'app-admin-reports',standalone:true,imports:[CommonModule,AdminShellComponent],templateUrl:'./reports.component.html',styleUrl:'./reports.component.scss'})export class ReportsComponent implements OnInit{svc=inject(AdminService);data:any={revenue:0,orders:0,users:0,nurseries:0,plants:0,customers:0,monthly:[],categories:[]};ngOnInit(){this.svc.reports().subscribe(r=>this.data=r)}max(){return Math.max(...this.data.monthly.map((x:any)=>x.value),1)}height(v:number){return Math.max(8,Math.round(v/this.max()*100))+'%'}download(){const rows=[['Metric','Value'],['Revenue',this.data.revenue],['Orders',this.data.orders],['Users',this.data.users],['Nurseries',this.data.nurseries],['Plants',this.data.plants],...this.data.monthly.map((x:any)=>[x.month,x.value])];const csv=rows.map((r:any)=>r.join(',')).join('\n');const a=document.createElement('a');a.href=URL.createObjectURL(new Blob([csv],{type:'text/csv'}));a.download='plantuno-report.csv';a.click()}}
